
#include <iostream>
#include<bits/stdc++.h>
#define ll long long
#define f(i, a, b) for (int i = a; i < b; i++)

using namespace std;

int main() {

int vertex;
int edges;
cin>>vertex>>edges;
vector <int> adj[vertex+1];
f(i,0,edges)
{int u,v; 
cin>>u>>v;
adj[u].push_back(v);
adj[v].push_back(u);

}

return 0;
	}
